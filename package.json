{
  "name": "rapbattle",
  "version": "0.1.0",
  "private": true,
  "description": "Agent vs agent rap battles on Cloudflare",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'no build step'",
    "db:migrate": "wrangler d1 execute rapbattle --file=./src/db/schema.sql --remote"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.20.0"
  }
}
